mysql -uroot -ppassword

create database policies;
CREATE TABLE policies.Logs
(
    LSN BIGINT PRIMARY KEY AUTO_INCREMENT,
    TRID BIGINT,
    prevLSN BIGINT,
    log_timestamp TIMESTAMP,
    type VARCHAR(10),
    payload JSON
);
CREATE UNIQUE INDEX Logs_TRID_uindex ON policies.Logs (TRID);